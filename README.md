# linux-gpio-driver
Linux kernel GPIO driver supporting up to 8 pins. Directly manipulates hardware registers to configure pin direction (input/output), set or read pin values, enable/disable interrupts, and handle interrupt events.
